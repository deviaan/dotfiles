# Basic compton setup
backend = "glx";
vsync = "opengl-swc";

glx-copy-from-font = true;
glx-swap-method = 2;
xrender-sync = true;
xrender-sync-fence = true;

opacity-rule = [
    "0:_NET_WM_STATE@:32a *= '_NET_WM_STATE_HIDDEN'"
];
